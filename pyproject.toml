[project]
name = "leap"
version = "0.1.0"
description = "A fast runner for table reasoning experiments."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.4.1",
    "vllm==0.10.0",
    "PyYAML>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
    "pytest-cov>=7.0.0",
]
